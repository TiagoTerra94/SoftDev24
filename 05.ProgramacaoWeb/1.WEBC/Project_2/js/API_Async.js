//fetch

//Seleciona o endpoint https://dummyjson.com/docs/products limitado a 10 produtos - limit

/* fetch('https://dummyjson.com/products?limit=10')
    .then(response =>{//espero pela resposta
        if(response.ok){//
            return response.json()
        }
    })
    .then(data => console.log(data)) //apos a resposta guardo os dados
    .catch(error => console.log(`Erro: ${error}`)) //caso der erro */

    /* const id = 24
    fetch(`https://dummyjson.com/products/${id}`)//busca o id
    .then(response =>{//espero pela resposta
        if(response.ok){//
            return response.json()
        }
    })
    .then(data => console.log(data)) //apos a resposta guardo os dados
    .catch(error => console.log(`Erro: ${error}`)) //caso der erro */